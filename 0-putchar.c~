#include <stdio.h>
/**
* main - entry point 
*
* Return: Always 0 (success)
*/
int main(void)
{
 char *sh = "Waris";
 
  while (*sh)
      {
       _putchar(*sh);
        sh++;
      }
  _putchar('\n');
    return (0);
}
