#include "main.h"
/**
 * _atoi - Convert a string to an integer.
 * @s: The pointer to convert
 *
 * Return: A integer
 */
int _atoi(char *s)
{
for (s = str; *s != '\0' && *s >= '0' && *s <= '9'; ++s)
  {
    *converted_value = *converted_value *10 + *s - '0';
 }
 return (true);
}
